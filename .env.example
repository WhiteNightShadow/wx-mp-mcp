# ── wx-mp-mcp 配置 ──
# 复制为 .env 后按需修改。所有项都有默认值，通常无需任何配置。

# 可选:手动指定微信小程序缓存目录(留空 = 按平台自动探测)。
# macOS:  ~/Library/Containers/com.tencent.xinWeChat/.../Applet/packages
# Windows: ~/Documents/WeChat Files/Applet
# WXAPKG_ROOT=
